exp_name: faster_rcnn
model_yaml: ultralytics/cfg/models/rt-detr/rtdetr-l.yaml   # 注意：Ultralytics 暂时没有 Faster R-CNN，可用其他框架
pretrained: null
data_yaml: configs/dataset.yaml
epochs: 300
imgsz: 640
batch: 8
device: '0'
optimizer: AdamW
lr0: 0.01
momentum: 0.937
workers: 8
seed: 42
hyperparams: {}